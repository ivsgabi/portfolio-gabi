// First Page - OS CHOICE

import { OSChoiceButton } from "@/components/OSChoiceButton";

export default function Home() {
  return (
    <main className="flex items-center justify-center h-screen">
      <OSChoiceButton></OSChoiceButton>
    </main>
  );
}
